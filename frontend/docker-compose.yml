version: '3.7'

services:
  frontend:
    build:
      context: .
      target: dev  
    ports:
      - "4200:4200"  
    volumes:
      - .:/app  
      - /app/node_modules  
    environment:
      - CHOKIDAR_USEPOLLING=true  # Pour la détection des changements de fichiers dans Docker
